rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Reglas para lectura pública de torneos
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
